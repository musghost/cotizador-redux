import {
  TOGGLE_LOGIN,
  ADD_LOGIN_ERRORS
} from '../constants/ActionTypes';

const initialState = {};

export default function quote(state = initialState, action) {
  switch (action.type) {

    default:
      return state;
  }
}
